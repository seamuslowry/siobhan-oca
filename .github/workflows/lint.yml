name: Code Quality
on:
  - push

jobs:
  lint:
    runs-on: ubuntu-latest
    name: NPM Lint
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22.22.2
      - run: npm install
      - run: npm run lint