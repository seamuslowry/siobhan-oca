name: Code Quality
on:
  - push

jobs:
  lint:
    runs-on: ubuntu-latest
    name: NPM Lint
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
      - run: npm install
      - run: npm run lint